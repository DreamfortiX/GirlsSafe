# Project-wide Gradle settings
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8

# Android configuration
android.useAndroidX=true
android.enableJetifier=true

# Gradle performance settings
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true

# Kotlin code style
kotlin.code.style=official

# Disable configuration cache temporarily
org.gradle.configuration-cache=false

# Network settings
systemProp.org.gradle.internal.http.connectionTimeout=120000
systemProp.org.gradle.internal.http.socketTimeout=120000

# Disable parallel execution if you encounter issues
# org.gradle.parallel=false

# Enable build scan (optional)
# com.gradle.enterprise.build-scan-plugin.allow-insecure-server=true
# com.gradle.enterprise.build-scan.pluginVersion=3.13.4